controller_list:
  - name: fake_cherry_bot_arm_controller
    type: $(arg fake_execution_type)
    joints:
      - joint1
initial:  # Define initial robot poses per group
#  - group: cherry_bot_arm
#    pose: home

  []